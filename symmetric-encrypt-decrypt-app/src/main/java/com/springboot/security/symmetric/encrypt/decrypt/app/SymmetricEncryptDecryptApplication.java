package com.springboot.security.symmetric.encrypt.decrypt.app;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SymmetricEncryptDecryptApplication {

    public static void main(String[] args) {
        SpringApplication.run(SymmetricEncryptDecryptApplication.class, args);
    }

}
